name: Cloud Watchdog
on:
  workflow_dispatch:
  repository_dispatch:
    types: [watchdog]
  schedule:
    - cron: "0 */4 * * *"
jobs:
  readiness:
    runs-on: ubuntu-latest
    concurrency:
      group: cloud-watchdog
      cancel-in-progress: false
    env:
      BASE44_APP_ID: ${{ secrets.BASE44_APP_ID }}
      BASE44_SERVICE_TOKEN: ${{ secrets.BASE44_SERVICE_TOKEN }}
      PAYPAL_WEBHOOK_ID: ${{ secrets.PAYPAL_WEBHOOK_ID }}
      PAYPAL_CLIENT_ID: ${{ secrets.PAYPAL_CLIENT_ID }}
      PAYPAL_CLIENT_SECRET: ${{ secrets.PAYPAL_CLIENT_SECRET }}
      PUBLIC_WEBHOOK_BASE_URL: ${{ secrets.PUBLIC_WEBHOOK_BASE_URL }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: "npm"
      - name: Install dependencies (with retries)
        run: |
          set -euo pipefail
          n=0
          until [ $n -ge 3 ]; do
            if npm ci --no-audit --no-fund 2>/dev/null; then break; fi
            n=$((n+1))
            echo "npm ci failed; retrying ($n)..."
            sleep 5
          done
      - name: Readiness ping (continue on error)
        run: npm run live:readiness:ping || echo "live:readiness:ping failed but continuing"
